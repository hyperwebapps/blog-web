import { Box, Toolbar } from "@mui/material"
import { Navbar } from "../components"

export const Homepage = () => {
  return (
    <Box>
      <Navbar />
      <Toolbar />
      {/* <Footer /> */}
    </Box>
  )
}
